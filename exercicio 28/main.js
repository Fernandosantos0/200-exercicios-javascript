'use strict';

// Variáveis
let soma = 0;
let i = 1;

// Estrutura condicional
while(i < 101) {
    soma += i;
    i++;
}

console.log('A soma de todos os números de 1 a 100 é igual a ' + soma + '.');