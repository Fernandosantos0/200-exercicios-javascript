'use strict';

function verificarPar(num) {

    if(num % 2 === 0) {
        console.log(num + ' é par');
    } else {
        console.log(num + ' é impar');
    }

}

verificarPar(0);
verificarPar(10);
verificarPar(5);
verificarPar(13);